import Link from "next/link";
import { getSettings } from "@/lib/database";
import { normalizeSiteSettings, type SiteSettings } from "@/lib/site-settings";

async function resolvedSite(site?:SiteSettings){if(site)return site;const settings=await getSettings();return normalizeSiteSettings(settings.site)}
export async function PublicHeader({site}:{site?:SiteSettings}={}){const current=await resolvedSite(site);const social=current.socialLinks.filter(link=>link.placement==="header"||link.placement==="both");return <header className="site-header page-shell"><Link className="wordmark" href="/" aria-label={`${current.name}, home`}><span aria-hidden="true">✦</span> {current.name}</Link><nav aria-label="Main navigation">{current.navigation.map(link=><Link href={link.href} key={`${link.label}-${link.href}`}>{link.label}</Link>)}<Link href="/search">ค้นหา</Link>{social.map(link=><a className="header-social" href={link.href} target="_blank" rel="noreferrer" key={`${link.platform}-${link.href}`}>{link.label} ↗</a>)}<Link className="nav-pill" href="/admin">Admin ↗</Link></nav></header>}
export async function PublicFooter({site}:{site?:SiteSettings}={}){const current=await resolvedSite(site);const social=current.socialLinks.filter(link=>link.placement==="footer"||link.placement==="both");return <footer><div className="page-shell footer-inner"><Link className="wordmark" href="/"><span aria-hidden="true">✦</span> {current.name}</Link><span>{current.footer}</span>{social.length>0&&<nav className="footer-social" aria-label="Social media">{social.map(link=><a href={link.href} target="_blank" rel="noreferrer" key={`${link.platform}-${link.href}`}>{link.label} ↗</a>)}</nav>}<a href="#top">BACK TO TOP ↑</a></div></footer>}
